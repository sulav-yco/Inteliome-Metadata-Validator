CREATE TABLE MOVIES (
    name VARCHAR,
    genre VARCHAR,
    year INT,
    released_date DATE,
    released_country VARCHAR,
    country VARCHAR,
    score DECIMAL(10,2),
    votes INT,
    budget DECIMAL(18,2),
    gross DECIMAL(18,2),
    director VARCHAR,
    writer VARCHAR,
    star VARCHAR,
    company VARCHAR,
    runtime INT
);
